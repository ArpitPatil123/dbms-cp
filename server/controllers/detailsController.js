export const detailsController = (req, res) => {
    const { id } = req.token;
    // from the id get the ngo of that user, volunteers of that ngo, events of that ngo using join and return all details to user
    // return all details to user
    // const q = 
    
};